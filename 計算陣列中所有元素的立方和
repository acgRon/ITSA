#include <stdio.h>

int main() {
    int array[6];
    for (int i = 0; i < 6; i++) {
        scanf("%d", &array[i]);
    }

    // 計算元素的立方和
    long long cubeSum = 0;
    for (int i = 0; i < 6; i++) {
        cubeSum += (long long)array[i] * array[i] * array[i];
    }

    printf("%lld\n", cubeSum);

    return 0;
}
