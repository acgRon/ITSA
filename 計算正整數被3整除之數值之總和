#include <stdio.h>

int main() {
    int N, sum = 0;
    scanf("%d", &N);

    // 計算可被 3 整除的數值之總和
    for (int i = 1; i <= N; i++) {
        if (i % 3 == 0) {
            sum += i;
        }
    }
    printf("%d\n",sum);

    return 0;
}
